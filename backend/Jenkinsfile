pipeline {
    agent {
        docker {
            image 'openjdk:22-jdk-slim'
        }
    }
    stages {
        stage('Docker Test') {
            steps {
                sh 'java -version'
            }
        }
    }
}